# Screenshots and JSON evidence are regenerated by every run of the browser
# suite (`python -m pytest tests/browser -m browser -q`). They are review
# material, not source — keep the directory, never the contents.
*
!.gitignore
