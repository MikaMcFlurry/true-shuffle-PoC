# UC-01–30 Evidence Matrix — TS-FABLE-01 (G4/E2 + §A-Abschluss)

**Datum:** 2026-08-01 · **Branch:** `claude/true-shuffle-fable5-lead-928f25` · **HEAD:** `7ae2520`
(*feat(ux): §A UI package — reactivation view, real favourite priority, per-track weight, honest requeue text*)

**Suiten (auf diesem HEAD vom Lead ausgeführt, nicht zitiert):**

| Lauf | Kommando | Ergebnis |
|---|---|---|
| Unit/API/Property/Sim (Default) | `python -m pytest -q` | **649 passed**, 62 deselected (`addopts = "-m 'not browser and not slow'"`, `pyproject.toml`), 78 s — enthält die §A-Neuzugänge `test_window_reassert.py` (5), `test_completion_flows.py` (3), `test_progress_stats.py` (8), `test_restart_e2e.py` (3), `test_apply_sync_http.py` (6), `test_retention.py` (6), `test_replan_fastpath.py` (4) |
| Browser (Playwright) | `ENABLE_DEMO_PROVIDER=true python -m pytest tests/browser -m browser -q` | **54 passed**, 351 s (50 + 4 aus `test_phase3_ui_controls.py`) |
| Slow (im Default-Lauf deselektiert) | `python -m pytest -m slow -q` | **8 passed**, ~2–3 s — deckt u. a. die als UC-06/UC-07-Beleg zitierten `test_p1_deep_cycles_up_to_500_cards` und `test_p7_quota_ceiling_over_five_thousand_draws[…]` ab |

**Evidenzklassen** (aus `handoffs/TS-FABLE-01/02_SOURCE_PRIORITY_AND_CONFLICT_RULES.md`):
`VERIFIED_CODE` (im Code nachgewiesen) · `VERIFIED_AUTOMATED` (durch automatisierte Tests) ·
`VERIFIED_LIVE` (echtes Konto + reales Gerät) · `OBSERVED_USER` · `INFERRED` · `UNVERIFIED` · `BLOCKED`.
„Test grün“ ist **nie** `VERIFIED_LIVE`.

**Status-Definition dieser Matrix (streng):**
- **PASS(automated)** = Verhalten implementiert **und** durch benannte, existierende Tests geprüft, Suite grün (= `VERIFIED_AUTOMATED`). Eine UI-Route allein zählt nicht.
- **TEILWEISE** = implementiert, aber mit exakt benannter Lücke (Test- oder Funktionslücke).
- **BLOCKED(live)** = Nachweis braucht echtes Spotify-Konto/Gerät; Verweis auf `LIVE_TEST_GUIDE.md` (LT-1…LT-14; LT-13/uris-Limit und LT-14/Planänderung-während-Wiedergabe haben seit 2026-08-01 eigene Guide-Abschnitte).
- **NEIN** = nicht implementiert.

Die Live-Spalte ist bewusst eigenständig: **das Live-Gate ist komplett BLOCKED** (keine Credentials,
siehe GATE_STATUS „Bekannte externe Blocker“). Jede automatisierte Zeile ruht zudem auf den
Simulator-Annahmen AN-1…AN-7 (`tests/sim_spotify.py`), die live unbestätigt sind.

---

## UC-01…UC-30 (gemäß `03A_CANONICAL_USE_CASES.md`)

| UC | Kurzname | Implementierung | Automatisierte Tests | Status | Live-Nachweis | Einschränkungen |
|---|---|---|---|---|---|---|
| 01 | Mit Spotify anmelden | `app/routes_auth.py::login/callback` (PKCE, State-Check), `providers/spotify.py::SCOPES`, `app/accounts.py`, `app/crypto.py` (Token-Versiegelung) | `test_api.py::test_connect_flow_stores_an_account`, `::test_callback_rejects_a_mismatched_state`, `::test_callback_without_a_started_flow_is_rejected`, `::test_disconnect_removes_the_account`; `test_db.py::test_tokens_are_encrypted_on_disk`; `test_crypto.py` (10 Tests) | PASS(automated) | BLOCKED (LT §1: echter PKCE-Roundtrip nie mit echtem Konto gelaufen; §1 zeigt auf die Voraussetzungen-Sektion, es gibt dort keinen eigenen LT-Ablauf für Connect/Playlist-Listing) | Dev-Mode-Grenzen: Premium-Owner nötig, max. 5 Nutzer je App (Policy Feb 2026); Spotifys PKCE-Ableitung (`providers/spotify.py:131–135`, `_code_verifier`/`_code_challenge`, S256) hat keine eigene automatisierte Assertion — alle zitierten Tests laufen gegen `FakeProvider` (`tests/conftest.py`), das `code_verifier` fest auf `"v"` setzt |
| 02 | Eigene Playlists anzeigen | `app/routes_api.py::GET /api/playlists`, `providers/spotify.py` (Pagination), `app/templates/library.html` | `test_api.py::test_playlists_are_listed_once_connected`, `::test_an_unreadable_playlist_is_listed_with_its_reason`; `test_providers.py::test_spotify_pages_within_the_limit_the_items_endpoint_allows` | PASS(automated) | BLOCKED (LT §1.6) | Fremde Playlists seit Feb 2026 per Spotify-Policy nicht lesbar — nur eigene |
| 03 | Playlist importieren | `app/library_service.py::import_playlist/upsert_playlist/content_hash`, `POST /api/playlists/{p}/{id}/import` | `test_library_service.py::test_import_persists_every_entry_with_duplicates_and_availability`, `::test_import_api_roundtrip_and_listing_status`, `::test_unreadable_playlist_import_is_refused_up_front`, `::test_provider_failure_mid_stream_marks_the_snapshot_failed` | PASS(automated) | BLOCKED (LT-9: 100+-Track-Playlist, Quota/429) | Import ohne feste Abspielreihenfolge (Snapshot) — vertragskonform |
| 04 | Playlist synchronisieren | `app/library_service.py::compute_sync_diff/apply_sync_to_run`, `POST …/sync`, `GET …/sync/latest`, Sync-UI `library.html:1005` (`loadSyncDiff`, inline `<script>` — **nicht** in `app/static/app.js`) | `test_library_service.py::test_second_import_diffs_add_remove_move_and_readded`, `::test_sync_api_imports_again_and_returns_the_diff`; `test_policies.py::test_apply_sync_is_idempotent`, `::test_apply_sync_readded_after_removal_restores_the_same_card`, `::test_apply_sync_refuses_a_foreign_diff`; `test_db.py::test_sync_available_is_true_once_a_newer_ready_snapshot_exists` | PASS(automated) | BLOCKED (Sync gegen real veränderte Spotify-Playlist) | Diff braucht zwei Imports; Hörstände bleiben laut Tests erhalten (readded → gleiche Karte). Kleinere Zitatschwäche: `test_apply_sync_refuses_a_foreign_diff` behauptet laut Docstring einen fremden Diff, übergibt aber einen nicht-existenten `diff_id` — der echte Fremd-Diff-Fall wurde nur adversarial nachgebaut (Guard hält), nicht vom benannten Test abgedeckt |
| 05 | Neuen Hörvorgang erstellen | `app/runs.py::create_run_v3`, `POST /api/runs`, Builder in `library.html:856` (`createConfigFor`) / `library.html:683` (`runPreflight`) — beide inline, **nicht** in `app/static/app.js` | `test_run_lifecycle.py::test_a_new_run_uses_the_newest_ready_snapshot`, `::test_create_run_v3_refuses_an_unreadable_playlist`, `::test_create_run_v3_refuses_a_foreign_config`, `::test_default_names_get_a_suffix_instead_of_an_integrity_error`; Browser: `test_phase3_flows.py::test_saving_a_config_in_the_builder_starts_the_run_bound_to_it` | PASS(automated) | BLOCKED (Start am echten Gerät, LT-1/LT-2) | — |
| 06 | Vollständig ohne Wiederholung | `core/selection.py::select_next/plan_cycle` (`no_repeat`), `app/runs.py::_book_advance`, Persistenz `run_tracks`/`run_plan` (`app/db.py`) | `test_selection_properties.py::test_p1_no_repeat_cycle_selects_every_card_exactly_once`; `test_api.py::test_no_track_repeats_across_a_whole_deck`; `test_selection.py::test_no_repeat_exhausts_when_nothing_is_open`; **zusätzlich, `@pytest.mark.slow`, im Default-Lauf deselektiert, separat grün via `python -m pytest -m slow -q`:** `test_selection_properties.py::test_p1_deep_cycles_up_to_500_cards` | **TEILWEISE** (Runde-2-Befund B3: einheitliche Statusregel — die dokumentierte Endgame-Ausnahme ist eine exakt benannte Funktionslücke gegen den kanonischen Wortlaut „jeder Song … einmal", 03A §6, und wird hier genauso bewertet wie in RUN-03) | BLOCKED (mehrtägiger Durchlauf am Gerät, LT-9) | Dokumentierte Ausnahme von „vollständig, keine Wiederholung“: ein `requeue_later`-Skip nahe Zyklusende kann eine Karte `deferred`/`play_count 0` zurücklassen, während der Run trotzdem `completed`/100 % meldet (siehe RUN-03) — bewusstes, getestetes Verhalten (`test_requeue_later_returns_no_earlier_than_the_deadline`); die Karte kommt im Folgezyklus/Replan zurück |
| 07 | Kontrollierte Wiederholungen | `core/selection.py::Rules` (`repeat_mode` free/limited, `min_gap`, `repeat_quota_pct`), `_quota_allows_repeat`; **pro Track (§A5, 2026-08-01):** `app/runs.py::set_track_weight` (0.1–10.0), `PUT /api/runs/{id}/tracks/{tid}/weight`, Gewichts-Zyklusknopf in „Als Nächstes” (`player.js::cycleWeight`, Stufen Selten 0.4 / Normal 1.0 / Öfter 2.0) | `test_selection.py::test_quota_boundary_is_strict`, `::test_free_repeat_ignores_the_quota`; `test_selection_properties.py::test_p7_quota_ceiling_holds_over_long_sequences`; slow-Ergänzungen wie zuvor; **pro Track:** `test_replan_fastpath.py::test_favorites_still_use_the_draw_loop` (Gewichtung erzwingt den Draw-Loop); Browser: `test_phase3_ui_controls.py::test_upnext_weight_control_cycles_to_more_and_persists` | PASS(automated) | BLOCKED (Hörbarkeit realer Wiederholungen) | „Nicht wiederholen” je Track bildet die UI bewusst als Ausschluss (UC-20) ab, nicht als Gewicht 0 — die Engine verlangt weight > 0 |
| 08 | Songs favorisieren | `app/runs.py::mark_favorite`, `POST/DELETE /api/runs/{id}/tracks/{tid}/favorite`, Player-Glyphen `player.js`; Gewichtung `core/selection.py::_effective_weight`; **UI (§A4, 2026-08-01):** Builder-Radios „Favoriten-Vorrang" echt (Aus 1.0 / Leicht 2.0 / Stark 4.0, `library.html`), Config-Editor-Select (`configs.html::editForm`) | `test_policies.py::test_favorite_weighting_changes_the_replanned_tail_deterministically`; `test_selection_properties.py::test_p4_favorites_win_significantly_more_often_while_p2_holds`, `::test_p4_weight_and_favorite_weight_compose`; Browser: `test_phase3_ui_controls.py::test_builder_favorite_weight_strong_saves_as_four` | PASS(automated) | BLOCKED (statistische Wirkung live) | Frühere Vorschau-Stufe „Nur Favoriten" bewusst zu „Stark" (4.0) umbenannt — reine Ausschluss-Filterung ist mit `favorite_weight` nicht abbildbar und wäre unehrlich benannt gewesen |
| 09 | Wiederholungsabstand | `core/selection.py` Gap-Check + dokumentierte Relaxation, `preflight`-Korrekturvorschlag | `test_selection.py::test_gap_check_is_strict_at_the_boundary`, `::test_min_gap_zero_allows_an_immediate_repeat`, `::test_gap_relaxation_at_the_boundary_eight_titles`, `::test_preflight_flags_unsatisfiable_min_gap_with_a_correction`; `test_selection_properties.py::test_p2_every_repeat_keeps_the_effective_gap` | PASS(automated) | BLOCKED (Grenzfall am echten Player) | Bei Erschöpfung greift dokumentierte Relaxation auf den maximal erfüllbaren Abstand (`test_p2_relaxation_is_documented_consistently`) |
| 10 | Konfiguration speichern | `POST /api/configs` (+ Versionierung `app/db.py`), `configs.html`, Builder-`saveConfigBtn` (`library.html:300`, Handler `library.html:964`, inline — **nicht** in `app/static/app.js`) | `test_policies.py::test_configs_api_crud_duplicate_and_preflight`; Browser: `test_phase3_flows.py::test_saving_a_config_in_the_builder_starts_the_run_bound_to_it` | PASS(automated) | gering — im LT-9-Gesamtlauf mitzuprüfen | Die beworbenen Preset-Bundles („Lange Autofahrt“, „Entdeckermodus“, „Nur Favoriten“, „Feierabend-Mix“, `library.html:109–130`) sind **reine UI-Vorschau** (`chip-preview`); eigenes Speichern/Wiederverwenden ist real |
| 11 | Hörvorgang starten | `app/runs.py::start` (uris-Fenster, ADR-002/S1), `POST /api/runs/{id}/start`, `providers/spotify.py::play`, `app/watcher.py::Watcher` | `test_engine.py::test_start_plays_the_first_card_as_a_uris_window`; `test_api.py::test_starting_a_run_hands_over_the_whole_uris_window`; `test_watcher.py` (13 gesammelte Tests, nicht 3); ergänzend, **nicht gegen App-Code** (s. Einschränkung): `test_strategy_candidates.py::test_candidates_complete_a_natural_run_without_duplicates` | PASS(automated) | BLOCKED (LT-1, LT-2; uris-Body-Limit LT-13) | Wiedergabe nur simuliert (`tests/sim_spotify.py`, AN-1…AN-7 deklariert). Die zusätzlich zitierte `test_strategy_candidates.py`-Evidenz läuft gegen `tests/forensics/strategy_bench.py` — Modul-Docstring: „nothing is integrated into the app code" — kein `app/`/`core/`-Import; die Kernaussage von UC-11 ruht trotzdem trägfähig auf `test_engine`/`test_api` |
| 12 | Pausieren | `app/runs.py::pause`, `POST /api/runs/{id}/pause` | `test_api.py::test_pausing_keeps_the_position_and_resuming_returns_to_it`; Browser: `test_flow_critical.py::test_critical_flow_deal_start_pause_resume_dashboard_history`, `test_takeover_drift.py::test_leaving_it_paused_keeps_the_position` | PASS(automated) | BLOCKED (LT-9 „Pause in Spotify und in True Shuffle“) | — |
| 13 | Stoppen | `app/runs.py::stop_run` (F1, v3-Status `stopped`), `POST /api/runs/{id}/stop` | `test_run_lifecycle.py::test_stop_resume_roundtrip_keeps_the_cursor`; `test_migrations.py::test_stopped_is_not_live_but_fully_resumable` | PASS(automated) | BLOCKED (Stop/Wiederöffnen über Tage) | — |
| 14 | Fortsetzen | `app/runs.py::resume_run`, `core/engine.py::resume_status`; Resume-Refusal bei spielendem Schwester-Run | `test_engine.py::test_resume_returns_to_the_stored_cursor_not_the_top`; `test_run_lifecycle.py::test_resume_refuses_while_another_controller_run_is_playing`; `test_api.py::test_asking_for_the_same_deck_again_resumes_it` | PASS(automated) | BLOCKED (LT-6 Prozessneustart, LT-9 Resume nach Stunden) | Nach Prozessneustart einmalige Fenster-Reassertion möglich (`_window_anchors` prozesslokal, `app/runs.py:66`) — Ledger bleibt korrekt, aber hörbarer Titel-Restart nicht ausgeschlossen |
| 15 | Neu starten (Reset) | `app/runs.py::reset_run` (F2: neuer Zyklus, Historie bleibt), `POST /api/runs/{id}/reset` | `test_run_lifecycle.py::test_reset_starts_cycle_two_with_a_new_plan_and_keeps_history`, `::test_reset_refuses_a_cancelled_run` | PASS(automated) | gering — im LT-9-Gesamtlauf mitzuprüfen | Reset für Legacy-Import-Runs ohne materialisiertes Deck wird ehrlich verweigert (RUN_STATE/D2) |
| 16 | Mehrere Hörstände je Playlist | v3-Schema (M-Migrationen lösen `idx_runs_one_live` ab), `app/runs.py::create_run_v3`, `app/db.py::list_runs` | `test_run_lifecycle.py::test_two_independent_runs_of_the_same_playlist_and_mode`, `::test_runs_of_the_same_deck_share_track_rows_but_not_state`; `test_db.py::test_two_live_runs_of_the_same_playlist_are_allowed`, `::test_cancelling_one_run_leaves_its_siblings_untouched`; `test_migrations.py::test_restart_does_not_resurrect_idx_runs_one_live`; Browser: `test_takeover_drift.py::test_dashboard_holds_several_runs_of_the_same_playlist` | PASS(automated) | BLOCKED (parallele Stände über Gerätetage) | Bewusst nur **ein** Run gleichzeitig `playing` (`idx_runs_one_playing`); Resume verweigert dann mit Erklärung |
| 17 | Spotify parallel verwenden | `core/engine.py::reconcile` (Drift ≠ Advance), `reconcile_history`, `app/runs.py::sync_from_history`, `app/watcher.py` (Backoff bei Drift) | `test_engine.py::test_playing_something_outside_the_deck_is_drift_not_an_advance`, `::test_jumping_to_another_card_of_the_same_deck_is_also_drift`; `test_history_sync.py` (25 Tests, nicht 13; u. a. `::test_tracks_from_another_playlist_do_not_move_the_deck`); ergänzend, **nicht gegen App-Code** (s. Einschränkung): `test_strategy_candidates.py::test_manual_queue_titles_play_and_are_never_displaced` | PASS(automated) | **BLOCKED (LT-7, LT-8, LT-10, LT-11)** — Kernannahmen AN-1/AN-5/AN-6 nur simuliert | Ob manuelle Queue-Titel den TS-Play-Override real überleben (AN-1) und ob die Queue vor dem Kontext spielt (AN-5), ist live **unbewiesen**; bei Falsifikation ändert sich die dokumentierte Semantik. Die `test_manual_queue_titles_…`-Evidenz stammt aus `tests/forensics/strategy_bench.py` (nicht in `app`/`core` integriert) — die Drift-Kernaussage selbst ruht auf `test_engine.py` |
| 18 | Verhalten bei manueller Nutzung | F8-Zustandsautomat: `app/runs.py::manual_tick/manual_decision`, `Rules.manual_use_policy/manual_wait_seconds`, `POST /api/runs/{id}/manual-decision`, Zustand-C-Banner `player.js:495–511` | `test_policies.py::test_f8_auto_resume_reasserts_the_window`, `::test_f8_auto_pause_pauses_without_commands`, `::test_f8_ask_awaits_the_decision_and_the_api_resolves_it`, `::test_f8_ask_decision_pause_keeps_the_position`, `::test_f8_timeout_suspends_the_run`, `::test_manual_decision_api_contract`; Browser: `test_takeover_drift.py` (7 Tests) | PASS(automated) | **BLOCKED — die gesamte MAN-01…05-Matrix ist live** (LT-7/LT-8/LT-9 Gerätewechsel) | Interpretation „nach der manuellen Queue“ muss laut 08 aus realer Spotify-Semantik abgeleitet werden — bis Live-Test offen; Timeout-Suspend (`manual_wait_seconds`) nur simuliert |
| 19 | Songs überspringen | `core/selection.py::apply_skip` (consume/keep_open/requeue_later/defer_to_end), Verbuchung + Recency-Stempel `app/runs.py::_book_advance` (Fix in HEAD `946ac45`) | `test_selection.py::test_consume_marks_the_card_played`, `::test_keep_open_keeps_the_card_in_the_pool`, `::test_requeue_later_defers_with_a_resubmission_instruction`, `::test_defer_to_end_defers_until_the_cycle_end`; `test_policies.py::test_requeue_later_returns_no_earlier_than_the_deadline`, `::test_keep_open_skip_stamps_the_touch_for_the_gap_check`; `test_selection_properties.py::test_p6_apply_skip_state_sequences`; `test_api.py::test_skipping_consumes_one_card` | PASS(automated) | BLOCKED (LT-3: zehn native Skips) | RUN_STATE-Restpunkt (a) — fehlender Skip-Recency-Stempel — ist mit `946ac45` geschlossen und getestet. Die Endgame-Semantik von `requeue_later` (Skip nahe Planende ⇒ Karte bleibt `deferred` bis Folgezyklus) ist bewusst und getestet; **der Builder-Text ist seit §A7 (2026-08-01) ehrlich:** „…kommt frühestens nach einer kurzen Karenz zurück — liegt der Skip zu nah am Ende, schafft er es nicht mehr in diesen Durchlauf, sondern erst in den nächsten." |
| 20 | Songs ausschließen | `app/runs.py::set_track_exclusion`, `POST /api/runs/{id}/tracks/{tid}/exclude`, Player-Glyphe `player.js::excludeTrack`; **ADR-004 (§A1, 2026-08-01):** `_replan_tail` vergisst den Fensteranker immer; die Route re-asserted das frische uris-Fenster sofort und nahtlos (`runs.reassert_window`, `position_ms` erhalten), Watcher-Selbstheilung als Rückfallpfad | `test_policies.py::test_exclude_and_reactivate_take_effect_immediately`; `test_selection_properties.py::test_p5_excluded_and_unadmitted_never_appear`; Browser: `test_phase3_flows.py::test_excluding_a_track_in_the_player_removes_it_from_up_next`; **Provider-Assertion während laufender Wiedergabe (rot vor Fix):** `test_window_reassert.py::test_excluding_an_upcoming_track_reasserts_the_window_without_it`, `::test_reassert_observes_instead_of_fighting_when_the_listener_took_over`, `::test_a_failed_reassert_is_honest_and_the_next_advance_converges`, `::test_watcher_reasserts_a_missing_window_while_the_expected_track_plays` | PASS(automated) | BLOCKED (LT-14: Hörbarkeit/`position_ms`-Verhalten des Sofort-Re-Asserts live unbestätigt; Risiko von „hoch" auf „simulator-mitigiert" gesenkt) | Import-Ausschlüsse (unplayable) sind bewusst nicht nutzer-revozierbar (`test_import_exclusions_are_not_user_revocable`) |
| 21 | Ausgeschlossene reaktivieren | API: `DELETE /api/runs/{id}/tracks/{tid}/exclude`, `app/runs.py::set_track_exclusion`; **UI (§A3, 2026-08-01):** Player-Sektion „Ausgeschlossene Titel" (Disclosure, `player.html` + `player.js::loadExcluded/renderExcluded/reactivateTrack`) mit „Wieder aufnehmen"-Knopf je `excluded_user`-Zeile; Datenquelle `GET /api/runs/{id}/tracks?state=excluded` (neue Deck-Listing-Route) | `test_policies.py::test_exclude_and_reactivate_take_effect_immediately`, `::test_run_rules_and_track_actions_api_contract`; Browser (deckt auch die frühere Beleglücke „kommt real zurück in Als Nächstes"): `test_phase3_ui_controls.py::test_excluding_then_reactivating_a_track_returns_it_to_up_next`, `::test_import_excluded_tracks_have_no_reactivate_button` | PASS(automated) | gering (Reaktivieren-Fenster-Re-Assert live: LT-14 Schritt 3) | `excluded_rule`-Zeilen zeigen ehrlichen Hinweis statt eines Knopfs, der mit 409 scheitern würde. Restnotiz (Runde-2-Befund B10): `test_import_excluded_tracks_have_no_reactivate_button` kann sich selbst skippen, wenn die Demo-Playlist zufällig keine `excluded_rule`-Zeile erzeugt — bisher in keinem Lauf eingetreten; Fixture-Härtung als G5-Kleinpunkt notiert |
| 22 | Hörfortschritt ansehen | `app/runs.py::describe` (cursor/total/remaining/progress_pct/current/upcoming), `GET /api/runs/{id}` (+`skipped_count`, `repeat_count`, `excluded_count` via `db.deck_stats`), Kacheln in `history.html` | Bisherige indirekte Belege plus **unabhängige §A2-Runde (2026-08-01), 8 Tests:** `test_progress_stats.py::test_deck_stats_repeats_counts_repeat_events_not_repeated_cards` (5×-Karte ⇒ 4, Maximal-Kontrast zur verworfenen Lesart), `::test_deck_stats_repeats_accumulate_over_several_cards`, `::test_deck_stats_repeats_match_the_sql_definition_over_the_real_path`, `::test_deck_stats_excluded_counts_import_and_user_exclusions`, `::test_deck_stats_without_a_materialised_deck_reports_unknown_not_zero` (None statt falscher 0), `::test_requeue_later_endgame_completes_at_100_percent_with_an_unplayed_card`, `::test_run_state_endpoint_mirrors_deck_stats_and_the_skipped_list`, `::test_repeat_count_over_the_wire_grows_with_the_real_repeats` | PASS(automated) | gering | **Zählsemantik entschieden (2026-08-01, `app/db.py::deck_stats`-Docstring):** `repeats` zählt Wiederhol-VORGÄNGE (`SUM(play_count-1)`), nicht wiederholte Karten — per Test gepinnt. `progress_pct` zählt VERBRAUCHTE Karten (`cursor/total`), keinen Sekundenfortschritt (dokumentiert + asserted). **Zyklusgrenzen (Runde-2-Befund B5, entschieden):** `repeats` zählt ÜBER ALLE ZYKLEN — ein zweiter voller no_repeat-Durchlauf nach F2-Reset meldet ehrlich `deck_size` Wiederhol-Vorgänge (jeder Titel einmal wiederholt gehört); begründet im `db.deck_stats`-Docstring, gepinnt in `test_progress_stats.py::test_repeats_count_across_cycle_boundaries_by_design` |
| 23 | Verlauf ansehen | `GET /api/runs/{id}/events`, `app/templates/history.html` (mit Tracknamen seit D4) | `test_api.py::test_run_events_record_the_reason_for_every_move`, `::test_history_page_renders_for_your_own_run`; `test_db.py::test_events_record_how_the_cursor_moved`, `::test_skipped_entries_keep_their_reason`; Browser: `test_phase3_flows.py::test_history_shows_real_track_names_not_only_positions` | PASS(automated) | gering | — |
| 24 | Abschluss erkennen | `core/engine.py` Completion-Pfade, `app/runs.py::_finish`; danach: Reset (F2), **Regeländerung mit Reopen (§A6, 2026-08-01):** `change_run_rules` + `_replan_tail(allow_completed=True)` — eröffnen die neuen Regeln weitere Titel, wird der Lauf ehrlich als `stopped` wiedereröffnet (Antwort `reopened`/`status`, Event `reopened`); eröffnen sie nichts, sagt die Antwort das ehrlich | Bisherige Belege plus **rot-vor-Fix-Flows (2 von 3; der dritte ist ein Nichtregressions-Pin):** `test_completion_flows.py::test_allowing_repeats_after_completion_reopens_the_run` (Reopen + Resume + Start spielt die neuen Titel — seit Runde-2-Fix B8 inkl. Provider-Assertion `play_windows`; Verlauf unangetastet), `::test_a_rule_change_that_opens_nothing_stays_honestly_completed`, `::test_a_new_cycle_after_completion_starts_without_a_slot_conflict` (Reset+Start gepinnt grün) | **TEILWEISE** (Runde-2-Befund B3: einheitliche Statusregel — die kanonische Abschluss-Definition „ALLE berücksichtigten Songs … abgespielt", 03A §24, gilt wegen der Endgame-Ausnahme nicht ausnahmslos; gleiche Bewertung wie RUN-03) | BLOCKED (LT-9: Ende eines echten No-Repeat-Durchlaufs; Reopen live: LT-14 Schritt 8) | Die dokumentierte Abschluss-Ausnahme (requeue_later-Endgame, s. RUN-03) bleibt bewusst bestehen und ist jetzt zusätzlich durch `test_progress_stats.py::test_requeue_later_endgame_…` gepinnt |
| 25 | Neue Songs in laufenden Run | `app/library_service.py::apply_sync_to_run` (`include_now`/`after_cycle`/`ignore`), `POST /api/runs/{id}/apply-sync` (Policy-Override, `advance_lock`, seit §A1 mit `window`-Ausgang) | `test_policies.py`-Belege wie zuvor; **HTTP-Draht (unabhängige §A9-Runde, 6 Tests):** `test_apply_sync_http.py::test_apply_sync_include_now_adds_and_removes_over_the_wire`, `::test_apply_sync_after_cycle_parks_the_card_and_leaves_the_order_alone`, `::test_apply_sync_rejects_a_nonsense_policy_and_a_bad_diff_id` (400/409/404 real gepinnt), `::test_apply_sync_is_idempotent_over_the_wire`, `::test_apply_sync_reports_an_honest_window_outcome_when_nothing_plays`, `::test_apply_sync_reasserts_the_window_while_the_run_is_actually_playing` | PASS(automated) | BLOCKED (Sync gegen real wachsende Playlist; Sofort-Re-Assert: LT-14 Schritt 5) | Sync-UI (`library.html`, `#syncPolicySelect`) weiterhin ohne eigenen Browser-Test — Restnotiz, kein Statusverlust: der Draht-Vertrag dahinter ist vollständig gepinnt |
| 26 | Hörvorgang löschen | `app/runs.py::delete_run` (soft) / `hard_delete_run`, `DELETE /api/runs/{id}`, Papierkorb-Zweitschritt `runs.html:165–206` | `test_run_lifecycle.py::test_delete_soft_then_hard_leaves_the_library_untouched`, `::test_api_lifecycle_stop_resume_reset_delete`; `test_db.py::test_deleting_a_user_cascades_to_their_runs` | PASS(automated) | gering (Playlist-Unversehrtheit live trivial prüfbar) | **Papierkorb ist session-lokal:** archivierte Runs verschwinden nach Reload aus der Liste (`db.list_runs` filtert `archived_at`); endgültig löschen geht nur, solange die Karte noch auf der Seite liegt — kein persistenter Papierkorb-View |
| 27 | Konfiguration bearbeiten | `PATCH /api/configs/{id}` (Versionierung), laufende Runs: `app/runs.py::change_run_rules` (effective_from, Tail-Replan); **ADR-004 (§A1):** die Route re-asserted das Fenster sofort, wenn sich das hörbare Fenster geändert hat (`window`-Ausgang `reasserted`/`unchanged`) | Bisherige Belege plus Provider-Assertion (rot vor Fix): `test_window_reassert.py::test_a_rule_change_reasserts_only_when_the_audible_window_changed`; UI: Config-Editor inkl. neuem `favorite_weight`-Feld (Browser-Nichtregression `test_phase3_flows.py::test_duplicating_a_config_on_the_configs_page`) | PASS(automated) | BLOCKED (LT-14 Schritt 4; Risiko von „hoch" auf „simulator-mitigiert" gesenkt) | Historie bleibt unangetastet — getestet. Der `unchanged`-Pfad ist seit Runde-2-Fix B4 deterministisch gepinnt: `test_window_reassert.py::test_a_repeated_identical_rule_change_is_unchanged_and_sends_nothing` (zweiter identischer Patch ⇒ `unchanged`, kein Kommando) |
| 28 | Konfiguration duplizieren | `POST /api/configs/{id}/duplicate`, `configs.html` | `test_policies.py::test_configs_api_crud_duplicate_and_preflight`; Browser: `test_phase3_flows.py::test_duplicating_a_config_on_the_configs_page` | PASS(automated) | — (rein lokal) | — |
| 29 | Konfig auf andere Playlist | Config ist playlist-neutral (`Rules` ohne Track-Bezug, ADR-003/F7); `POST /api/runs` mit fremder `config_id` derselben Nutzerin | `test_policies.py::test_config_applies_to_another_playlist_without_track_rules`; `test_run_lifecycle.py::test_create_run_v3_refuses_a_foreign_config` (Ownership) | PASS(automated) | — (rein lokal) | Track-gebundene Regeln (Favoriten/Ausschlüsse) wandern bewusst **nicht** mit — sichere Übertragung |
| 30 | Lange Autofahrt (Komposit) | Komposition aus UC-03/05/10/11/13/14; Persistenz `app/db.py`, Restart-Pfad `core/engine.py::resume_status` | Bausteine einzeln (s. o.); **Restart-E2E gegen echte DB+App (§A8, unabhängige Runde, 2026-08-01):** `test_restart_e2e.py::test_restart_keeps_cursor_status_and_order_and_finishes_the_run` (Service: `close_db`/`init_db` auf derselben Datei, F5-Eindeutigkeit, frisches uris-Fenster nach Neustart, bis `completed`), `::test_restart_after_a_stopped_session_resumes_instead_of_restarting` (F1-Tagesgrenze), `::test_restart_over_http_resumes_the_same_run_and_plays_it_to_the_end` (zwei sequentielle TestClient-Lifespans: DB-Verbindung neu, prozesslokale Register im Test explizit geleert — ein echter Prozessneustart ist damit **simuliert**, nicht ausgeführt; Präzisierung Runde-2-Befund B6. Verlauf lückenlos); Browser-E2E mit Demo-Provider wie zuvor | PASS(automated) | **BLOCKED (LT-6, LT-9: Resume nach mehrstündigem Unterbruch, mehrere Fahrten)** — der Produkt-Kernnutzen hängt weiterhin am live-blockierten Nachweis | Hörbarer Titel-Restart nach Prozessneustart durch Watcher-Selbstheilung (ADR-004, positions-erhaltend) geschlossen — live unbestätigt (LT-14) |

---

## RUN-01…RUN-12 (Zusatzpflicht aus `08_ACCEPTANCE_TEST_MATRIX.md` §G4)

| RUN | Prüfung | Implementierung | Automatisierte Tests | Status | Live-Nachweis | Einschränkungen |
|---|---|---|---|---|---|---|
| RUN-01 | Zwei Runs derselben Playlist, isolierte Fortschritte | v3-Schema, `create_run_v3`, geteilte `run_tracks`-Rows aber getrennter State | `test_run_lifecycle.py::test_two_independent_runs_of_the_same_playlist_and_mode`, `::test_runs_of_the_same_deck_share_track_rows_but_not_state`; `test_db.py::test_two_live_runs_of_the_same_playlist_are_allowed`, `::test_cancelling_one_run_leaves_its_siblings_untouched`; Browser: `test_takeover_drift.py::test_dashboard_holds_several_runs_of_the_same_playlist` | PASS(automated) | BLOCKED (zwei Stände am echten Gerät) | nur ein Run gleichzeitig `playing` (gewollt) |
| RUN-02 | Run über Neustart fortsetzen | Cursor/Deck in DB; `resume_run`; F5-`event_keys` gegen Doppelverbuchung; Watcher-Selbstheilung re-asserted ein fehlendes Fenster positions-erhaltend (ADR-004) | **Benannte, dauerhafte Tests (§A8, 2026-08-01):** `test_restart_e2e.py` (3 Tests, Service- und HTTP-Ebene, s. UC-30) — Cursor/Status/Order exakt erhalten, `run_selections`-Eindeutigkeit (F5), keine `applied=0`-Doppelverbuchung, frisches uris-Fenster beim ersten Start nach Neustart; ergänzend wie zuvor `test_policies.py::test_f5_duplicate_event_key_is_applied_exactly_once` | PASS(automated) | BLOCKED (LT-6) | Der frühere „einmalige hörbare Titel-Restart" nach Neustart ist durch die Watcher-Selbstheilung (`test_window_reassert.py::test_watcher_reasserts_a_missing_window_while_the_expected_track_plays`) geschlossen; live unbestätigt (LT-14) |
| RUN-03 | No-Repeat bis Abschluss, jeder Track exakt 1× | `core/selection.py` P1-Vertrag | `test_selection_properties.py::test_p1_no_repeat_cycle_selects_every_card_exactly_once`, `::test_p1_plan_cycle_is_a_complete_permutation`, `::test_p1_five_hundred_cards_stay_exact`; `test_api.py::test_no_track_repeats_across_a_whole_deck` | **TEILWEISE** (korrigiert von PASS(automated)) | BLOCKED (LT-9 Durchlauf-Ende) | **Dokumentierte Lücke (neu):** die Aussage „jeder Track exakt 1×" gilt nicht unbedingt. `test_policies.py::test_requeue_later_returns_no_earlier_than_the_deadline` (neu in HEAD `946ac45`) pinnt als SOLL: ein `requeue_later`-Skip nahe Zyklusende lässt die Karte `deferred`/`play_count 0` liegen, während der Run trotzdem `status: completed` erreicht. Adversariale Sonde über den echten Advance-Pfad (`create_run_v3` → 8× `TRACK_ENDED` → 1× `USER_SKIP` → auslaufen lassen): `status: completed`, 11 von 12 `run_tracks` `state='played'`, `runs.describe` meldet `remaining: 0`, `progress_pct: 100.0` trotz einer nie gespielten, berücksichtigten Karte. Bewusstes, getestetes Verhalten — die Karte kehrt im Folgezyklus/Replan zurück; betrifft in derselben Weise UC-06, UC-19 und UC-24 |
| RUN-04 | Mindestabstand exakt an der Grenze | Gap-Check `core/selection.py` | `test_selection.py::test_gap_check_is_strict_at_the_boundary`, `::test_preflight_accepts_min_gap_exactly_at_the_boundary`; `test_selection_properties.py::test_p2_every_repeat_keeps_the_effective_gap`, `::test_attack_relaxed_gap_is_always_the_maximum_satisfiable_k` | PASS(automated) | BLOCKED | Relaxation bei Erschöpfung dokumentiert und getestet — kein stiller zu-früher Repeat |
| RUN-05 | Unmögliche Regelkombination vor Start erklärt | `core/selection.py::preflight`, `POST /api/runs/preflight`, Builder-Preflight (`library.html:683`, inline — **nicht** in `app/static/app.js`) | `test_selection.py::test_preflight_flags_unsatisfiable_min_gap_with_a_correction`, `::test_preflight_flags_quota_zero_with_limited_repeat`, `::test_preflight_flags_the_empty_candidate_set`; API-Aufruf in `test_policies.py::test_configs_api_crud_duplicate_and_preflight` (`tests/test_policies.py:1006`) | PASS(automated) | — (rein lokal) | — |
| RUN-06 | Skip-Policy, alle vier Varianten | `apply_skip` + Verbuchung `_book_advance` (Recency-Stempel HEAD `946ac45`) | `test_policies.py::test_skip_policy_consume_marks_the_card_played`, `::test_skip_policy_keep_open_leaves_the_card_in_the_pool`, `::test_skip_policy_deferral_extends_the_plan_at_the_end`, `::test_requeue_later_returns_no_earlier_than_the_deadline`, `::test_keep_open_skip_stamps_the_touch_for_the_gap_check`; `test_selection_properties.py::test_p6_requeue_later_card_returns_exactly_once`, `::test_p6_defer_to_end_cards_play_only_after_the_open_pool` | PASS(automated) | BLOCKED (LT-3 native Skips) | Siehe UC-19: Endgame-Semantik von `requeue_later` bewusst und getestet; Builder-Text seit §A7 ehrlich formuliert |
| RUN-07 | Favoritengewichtung statistisch, Mindestabstand hart | `_effective_weight`/`_weighted_pick` | `test_selection_properties.py::test_p4_favorites_win_significantly_more_often_while_p2_holds`, `::test_p4_weight_and_favorite_weight_compose`, `::test_attack_extreme_weights_keep_every_candidate_representable`; `test_policies.py::test_favorite_weighting_changes_the_replanned_tail_deterministically` | PASS(automated) | BLOCKED | favorite_weight ist seit §A4 per Builder/Config-Editor setzbar (s. UC-08) — die frühere UI-Lücken-Einschränkung ist aufgelöst (Runde-2-Befund B7) |
| RUN-08 | Ausschließen/Reaktivieren sofort, run-spezifisch | `set_track_exclusion` (run-scoped, F7) + ADR-004-Sofort-Re-Assert (§A1) | Wie zuvor plus `test_window_reassert.py` (4 Provider-Assertionen, rot vor Fix) und Browser `test_phase3_ui_controls.py::test_excluding_then_reactivating_a_track_returns_it_to_up_next` | PASS(automated) | BLOCKED (LT-14; Risiko von „hoch" auf „simulator-mitigiert" gesenkt) | Reaktivieren-UI seit §A3 vorhanden (s. UC-21) |
| RUN-09 | Playlist-Sync: Add/Remove, Fortschritt erhalten, Nutzerentscheidung | `compute_sync_diff` + `apply_sync_to_run` + Policy-Override im API-Body | Wie zuvor plus die 6 HTTP-Draht-Tests aus `test_apply_sync_http.py` (§A9, s. UC-25) — inkl. real gepinnter Statuscodes (unbekannter Diff ⇒ **409**, nicht 404) und Idempotenz über den Draht | PASS(automated) | BLOCKED (echte Playlist-Änderung; LT-14 Schritt 5) | — |
| RUN-10 | Neue Tracks im laufenden Run: alle drei Optionen | `NEW_TRACKS_POLICIES` (`include_now`/`after_cycle`/`ignore`), `apply_sync_to_run` | `test_policies.py::test_apply_sync_include_now_adds_and_removes`, `::test_apply_sync_after_cycle_parks_the_card_for_the_next_cycle`, `::test_apply_sync_ignore_creates_nothing` | PASS(automated) | BLOCKED | — |
| RUN-11 | Config duplizieren/übertragen: unabhängige Identität, sichere Track-Regeln | `POST /api/configs/{id}/duplicate`; playlist-neutrale `Rules` (F7) | `test_policies.py::test_configs_api_crud_duplicate_and_preflight`, `::test_config_applies_to_another_playlist_without_track_rules`; Browser: `test_phase3_flows.py::test_duplicating_a_config_on_the_configs_page` | PASS(automated) | — (rein lokal) | — |
| RUN-12 | Löschen: nur Ziel-Run, Spotify-Playlist unverändert | `delete_run`/`hard_delete_run`; kein Schreibpfad zur Playlist im Live-Modus | `test_run_lifecycle.py::test_delete_soft_then_hard_leaves_the_library_untouched` (trägt die Zeile); `test_db.py::test_deleting_a_user_cascades_to_their_runs` (Zitat-Hinweis: prüft den Cascade beim Löschen eines NUTZERS, nicht „nur Ziel-Run bleibt unberührt" — Füllzitat, keine Statusverletzung) | PASS(automated) | gering (Playlist-Unversehrtheit am Konto nachprüfbar) | Papierkorb session-lokal (s. UC-26) |

---

## Querschnittliche, ehrliche Einschränkungen

1. **Live-Gate komplett BLOCKED.** Keine Zeile dieser Matrix ist `VERIFIED_LIVE`. Voraussetzungen in `GATE_STATUS.md` §Blocker; nachholbar über `LIVE_TEST_GUIDE.md` (LT-1…LT-14; LT-13 und LT-14 haben seit 2026-08-01 eigene Abschnitte).
2. **Simulator-Annahmen AN-1…AN-7** (`tests/sim_spotify.py`) tragen alle Wiedergabe-/Queue-Aussagen; AN-1/AN-5/AN-6 sind live unbestätigt, Falsifikationsfolgen sind im Guide je LT beschrieben.
3. **Preset-Bundles nur UI-Vorschau** (`library.html`, `chip-preview`): „Lange Autofahrt“, „Entdeckermodus“, „Nur Favoriten“, „Feierabend-Mix“ sind nicht klickbar-real; eigene Configs speichern/duplizieren/übertragen ist real und getestet.
4. **Aufgelöst (§A4, 2026-08-01):** Favoriten-Vorrang ist real — Builder-Radios schreiben `favorite_weight` (1.0/2.0/4.0) in die Config, der Config-Editor kann ihn ändern; Browser-Test belegt die Speicherung. Die frühere „Nur Favoriten"-Vorschau-Stufe wurde bewusst zu „Stark" umbenannt (Ausschluss-Filterung wäre mit einem Gewicht unehrlich beworben).
5. **Papierkorb session-lokal** (UC-26): kein persistenter Papierkorb-View; nach Reload sind archivierte Runs nur noch per API erreichbar.
6. **M009 (order_json-Drop) bleibt gated** hinter dem destruktiven Flag (`test_migrations.py::test_m009_only_runs_behind_the_destructive_flag`), bis der v3-Pfad produktiv bestätigt ist.
7. **`_window_anchors` bleibt prozesslokal, aber das Restwissen ist entschärft (ADR-004):** nach Prozessneustart re-asserted der Watcher das Fenster selbstheilend und positions-erhaltend, sobald der erwartete Titel nachweislich spielt — der frühere einmalige hörbare Titel-Restart entfällt (getestet; live LT-14). Eine durable Anker-Variante ist damit bewusst nicht mehr geplant.
8. **Policy/Kommerz:** Streaming-SDA (Policy IV.2) — keine kommerziellen Claims ohne Prüfung; fremde Playlists nicht lesbar (Feb 2026).
9. **Aufgelöst (§A2, 2026-08-01):** `repeat_count`/`excluded_count`/`skipped_count`/`progress_pct` sind durch die unabhängige 8-Test-Runde (`test_progress_stats.py`) gepinnt. Zählsemantik ENTSCHIEDEN und implementiert: `deck_stats.repeats` zählt Wiederhol-VORGÄNGE (`SUM(play_count-1)`) — die frühere Fassung dieser Notiz beschrieb die Vor-Entscheidungs-Semantik.
10. Die früher notierte sporadische Fixture-Flakiness ist aufgeklärt und behoben (`71b2aa9`): prozessglobale `_advance_locks` leakten `asyncio.Lock`-Objekte über Event-Loop-Grenzen (Run-IDs starten je Test-DB bei 1); das autouse-Registry-Fixture leert sie jetzt wie die Fensteranker. Seither 3× Browser-Vollsuite in Folge grün (50/50, 54/54, 54/54).
11. **Implementierungs-Zitate auf `app.js` waren durchgängig falsch** (UC-04, UC-05, UC-10, RUN-05): `app/static/app.js` hat 264 Zeilen und enthält nur `labelTheme`/`pollJob` — `loadSyncDiff`, `createConfigFor`, `runPreflight` und `#saveConfigBtn` liegen alle inline in `app/templates/library.html`. Korrigiert in den jeweiligen Zeilen dieser Matrix.
12. **No-Repeat-Vollständigkeit hat eine dokumentierte Ausnahme** (RUN-03, UC-06, UC-19, UC-24): ein `requeue_later`-Skip nahe Zyklusende kann eine Karte `deferred`/`play_count 0` zurücklassen, während der Run trotzdem `completed`/100 % meldet. Bewusstes, getestetes Verhalten — seit §A zusätzlich end-to-end gepinnt (`test_progress_stats.py::test_requeue_later_endgame_completes_at_100_percent_with_an_unplayed_card`); der Builder-Text ist seit §A7 ehrlich formuliert. Die Karte kehrt im Folgezyklus/Replan zurück.
13. **`tests/forensics/strategy_bench.py` / `test_strategy_candidates.py` sind nicht in `app`/`core` integriert** (Modul-Docstring: „nothing is integrated into the app code“). Als Evidenz für UC-11, UC-17, UC-30 und RUN-02 nur mit diesem Vorbehalt zu lesen — die App-seitige Kernaussage der jeweiligen Zeile ruht auf den übrigen, echten Tests; für RUN-02/UC-30 (Restart+DB-Persistenz) fehlt dagegen ein tragfähiger benannter Test, daher dort auf TEILWEISE korrigiert.
14. **Aufgelöst (§A1/ADR-004, 2026-08-01):** Ausschluss, Reaktivierung, Regeländerung und Sync-Anwendung re-asserten das uris-Fenster jetzt sofort (nahtlos, `position_ms` erhalten), mit Anker-Invalidierung als Sicherheitsnetz und Watcher-Selbstheilung; rot-zuerst-Suite `tests/test_window_reassert.py`. Live-Bestätigung des `position_ms`-Verhaltens: LT-14. Zusätzlich (Phase-4-Profil): der Replan selbst war bei 10k Tracks 54,6 s unter dem `advance_lock` — reiner no_repeat-Fall jetzt O(n log n)/1,4 s (`test_replan_fastpath.py`); der gewichtete Fall bleibt O(n²) (Worknote PHASE4_PERF_PROFILE.md). **Runde-2-Befund B1 (kritisch, behoben):** die erste Fast-Path-Fassung (`16a12b1`) war gap-blind — offene Karten MIT Hör-Historie (keep_open-Skip) konnten innerhalb ihres min_gap-Fensters zurückkommen (gemessen 11–18/25 P2-Verletzungen). Seit dem Guard (min_gap>0 + Historie ⇒ Draw-Loop, gepinnt in `test_keep_open_history_with_min_gap_forces_the_draw_loop`) wieder 0 Verletzungen; betrifft rückwirkend keine ausgelieferte Version (beide Commits liegen im selben unreleased Branch-Abschnitt).

---

## Unabhängige Verifikation

Die Matrix wurde nach dem Bau von drei unabhängigen, adversarialen Prüfern gegengeprüft (je eigener
Testlauf auf HEAD `946ac45` + Arbeitsbaum, ohne Änderungen an dieser Datei). Alle drei bestätigen: kein
erfundener Testname (163/163 zitierte Node-IDs lösen auf reale `def test_…` auf), keine Zeile behauptet
`VERIFIED_LIVE`. Ergebnis je Linse:

- **rules** (UC-06/07/08/09/19/20/21, RUN-03/04/06/07/08): 4 von 4 Widerlegungen eingearbeitet
  (Deselektions-Kopf, UC-06/UC-07-`slow`-Herkunft, RUN-03-Vollständigkeitslücke, UC-19/RUN-06-Builder-Versprechen);
  8 Bestätigungen halten unverändert (u. a. UC-08/UC-21-Weakest-Row-Details, UC-09/RUN-04-Grenzwerte).
- **lifecycle** (UC-03/04/05/10/13/14/15/16/25/26/27/28/29, RUN-01/02/09/10/11/12): 3 von 3 Widerlegungen
  eingearbeitet (RUN-02 auf TEILWEISE korrigiert, UC-22-Dateizitate korrigiert, `app.js`-Zitate in
  UC-04/05/10/RUN-05 korrigiert); 13 Bestätigungen halten (u. a. UC-15/16/26/27/29 vollständig, Suitenzahlen
  reproduziert).
- **live-honesty** (UC-01/02/11/12/17/18/22/23/24/30, RUN-02/07/08, Suiten-Header, Querschnitt): 6 von 6
  Widerlegungen (R1–R6) eingearbeitet (Header-Aufschlüsselung, UC-24-Verweigerung statt „ungetestet“,
  UC-20/27/RUN-08-Live-Risiko hochgestuft, UC-22-Implementierung, UC-11/17/30/RUN-02-Bench-Vorbehalt,
  Testzahlen `test_watcher.py`/`test_history_sync.py` korrigiert); 7 Bestätigungen halten (u. a. C3: keine
  Zeile behauptet Live-Evidenz; C4: BLOCKED/LT-Verweise lösen korrekt auf).

**Zusammenfassung nach Verifikationsrunde 1 (2026-08-01, vor §A-Umsetzung):** UC: 24 PASS(automated), 6 TEILWEISE
(UC-07/08/21/22/24/30), 0 NEIN; RUN: 10 PASS(automated), 2 TEILWEISE (RUN-02, RUN-03); drei Zeilen
(UC-20/27/RUN-08) mit hohem strukturellem Live-Divergenz-Risiko markiert.

---

## §A-Abschlussrunde (2026-08-01, Fortsetzungssession)

Alle in Runde 1 benannten Lücken wurden umgesetzt (Commits `c86ed42`…, Details in den Zeilen oben):
ADR-004-Fenster-Re-Assert inkl. Watcher-Selbstheilung (UC-20/27, RUN-02/08), UC-24-Reopen,
UC-07/08/21-UI+API, UC-22-Zählentscheidung + 8-Test-Runde, Restart-E2E (RUN-02/UC-30),
apply-sync-HTTP-Runde (UC-25/RUN-09), Builder-Textfix (UC-19/RUN-06), LT-13/LT-14-Guide-Abschnitte.

**Aktualisierte Zusammenfassung:** UC: **30 PASS(automated), 0 TEILWEISE, 0 NEIN**. RUN: **11
PASS(automated), 1 TEILWEISE (RUN-03)** — dort bleibt die bewusste, getestete und jetzt end-to-end
gepinnte No-Repeat-Endgame-Ausnahme als TEILWEISE stehen (Ehrlichkeit vor Optik: „jeder Track exakt 1×"
gilt nicht ausnahmslos; UC-06/24 tragen dieselbe Ausnahme als dokumentierte Einschränkung).
Live-Spalte unverändert durchgängig BLOCKED/„gering"/„— (rein lokal)", **0 `VERIFIED_LIVE`** — neu
hinzugekommene Sofortpfade (Re-Assert mit `position_ms`, Reopen) sind über LT-14 nachholbar.

## Unabhängige Verifikation Runde 2 (2026-08-01, adversarialer Opus-Review ohne Implementierungskontext)

**Bestätigt:** alle 168 zitierten Node-IDs real und korrekt zugeordnet; Rot-vor-Fix hält „härter als
behauptet" (5/5 bzw. 2/3 ehrliche Funktionsfehlschläge am jeweiligen Parent-Commit, unabhängig von der
conftest-Erweiterung); alle drei Suitenzahlen exakt reproduziert (649/8/54); 0 `VERIFIED_LIVE`;
LT-13/LT-14-Verweise lösen auf; ADR-004-Kohärenz in 5 von 6 Angriffspunkten (Anker-Invalidierung immer,
apply-sync-Umgehungshypothese falsifiziert, kein Watcher-Race, position_ms-Quellen korrekt, F8-Schutz).

**Widerlegungen (B1–B11), alle noch am selben Tag eingearbeitet:**
- **B1 (kritisch):** die erste Fast-Path-Fassung war gap-blind (11–18/25 P2-Verletzungen bei offenen
  Karten mit Hör-Historie) → Guard + Regressionspin (`test_keep_open_history_with_min_gap_forces_the_draw_loop`), Notiz 14 korrigiert.
- **B2:** UC-07-Beleg war fehlattribuiert/browser-only → `tests/test_track_weight.py` (3 Tests:
  Service-Vertrag inkl. Grenzen/Ownership, deterministische Replan-Wirkung, Draht) + no_repeat-Semantik-Einschränkung.
- **B3:** Statusregel vereinheitlicht → UC-06/UC-24 auf TEILWEISE (wie RUN-03), Zusammenfassung korrigiert.
- **B4:** `unchanged`-Pfad deterministisch gepinnt (`test_a_repeated_identical_rule_change_is_unchanged_and_sends_nothing`).
- **B5:** `repeats`-Zyklusgrenzen entschieden (über alle Zyklen), Docstring + Pin (`test_repeats_count_across_cycle_boundaries_by_design`).
- **B6:** „echter Prozessneustart" → „simuliert" präzisiert; Restart-Test räumt jetzt auch `_advance_locks` und pinnt die Ausgangslage.
- **B7:** RUN-07-Restnotiz, LT-Bereichsangabe (LT-1…14) und LT-13-Vorbehalt aktualisiert.
- **B8/B9:** Reopen-Flow mit Provider-Assertion; „rot-vor-Fix (2 von 3)"-Präzisierung.
- **B10:** Skip-Fragilität des excluded_rule-Browser-Tests als G5-Kleinpunkt notiert.
- **B11:** Flakiness-Notiz 10 auf die gefundene Wurzel (`71b2aa9`) umgestellt.

**Zusammenfassung nach Runde 2:** UC: **28 PASS(automated), 2 TEILWEISE (UC-06, UC-24), 0 NEIN** ·
RUN: **11 PASS(automated), 1 TEILWEISE (RUN-03)** — alle drei TEILWEISE tragen dieselbe, bewusste und
end-to-end gepinnte Endgame-Ausnahme (einheitliche Statusregel). Live unverändert 0 `VERIFIED_LIVE`.
